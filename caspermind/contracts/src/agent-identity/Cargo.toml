[package]
name = "agent-identity"
version = "0.1.0"
edition = "2021"

[dependencies]
odra-casper-lcf = { version = "1.1.0", default-features = false }
odra = { version = "1.1.0", default-features = false }

[lib]
crate-type = ["cdylib"]

[profile.release]
codegen-units = 1
lto = true
